# Old-host AuthUserFile path — disabled to prevent HTTP 500.
# Re-enable via hosting control panel if needed.

# AuthType Basic
# AuthUserFile "/home/collegeb/.htpasswds/public_html/admin/passwd"
# require valid-user
# AuthName "Site Admin - modify sections on the home page, etc"
